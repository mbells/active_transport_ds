# OSM-Toys

Fun utility functions for dealing with Open Street Maps.

# Developing

```
uv venv .venv
source .venv/bin/activate
uv pip install -e ".[dev]"

# To install:
uv pip install -e .
```

# Testing

```
source .venv/bin/activate
tox
```
